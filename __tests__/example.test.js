import renderer from 'react-test-renderer';
import React from 'react';
import { Text } from 'react-native';

describe('App', () => {
    it('has 1 child', () => {
        // Placeholder test
        expect(1).toBe(1);
    });
});
